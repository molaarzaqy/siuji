# Third-party packages in the dashboard ship references to source maps that are
# not included in their published npm packages. Disable source-map generation
# for production bundles so CRA does not report those missing files.
GENERATE_SOURCEMAP=false
